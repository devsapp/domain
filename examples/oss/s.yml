edition: 1.0.0          #  命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: domainApp         #  项目名称
access: default  #  秘钥别名

services:
  domain-test: #  服务名称
    component: ${path(../..)}  # 组件名称
    props: #  组件的属性值
      type: oss
      region: cn-hongkong
      bucket: wss-test-domain
